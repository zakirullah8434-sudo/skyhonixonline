{
  "name": "skyhonix-teacher-app",
  "version": "1.0.0",
  "description": "SkyHonix Teacher Portal - Android App",
  "main": "index.js",
  "scripts": {
    "build": "echo Build complete",
    "cap:init": "npx cap init 'SkyHonix Teacher' com.skyhonix.teacher --web-dir www",
    "cap:add": "npx cap add android",
    "cap:sync": "npx cap sync",
    "cap:open": "npx cap open android"
  },
  "dependencies": {
    "@capacitor/android": "^6.0.0",
    "@capacitor/cli": "^6.0.0",
    "@capacitor/core": "^6.0.0"
  }
}
